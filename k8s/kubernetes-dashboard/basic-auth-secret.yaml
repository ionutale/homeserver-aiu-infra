apiVersion: v1
kind: Secret
metadata:
  name: dashboard-basic-auth
  namespace: kubernetes-dashboard
type: Opaque
# Replace the placeholder line below with the output of: htpasswd -nbB <user> '<password>'
# Example generation (macOS with apache2-utils installed via brew install httpd):
#   htpasswd -nbB admin 'ChangeMe!' | sed -e 's/\$/\$\$/g'
# Then put the entire 'user:hash' line below (without surrounding quotes).
stringData:
  auth: |
  admin:$2y$05$WNF3BzO4DrEBc4TyaWUMwuguyHl7VmMSa7uNosA.wC/5Y66JUOo/.
  # If you rotate, replace the single line above with a new htpasswd output (keep single $ characters; no quotes needed because we’re in a block literal).
